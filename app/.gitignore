# ===================================================================
# React Native App · 忽略规则
# （与仓库根的 .gitignore 叠加生效，这里只管 app/ 内部）
# ===================================================================

# ---------- 依赖 ----------
node_modules/

# ---------- Metro / Expo ----------
.expo/
dist/
*.jsbundle

# ---------- 构建产物 ----------
# build/ 是**无斜杠前缀**，因此匹配 app/ 下任意层级的 build 目录 ——
# 包括 android/build/ 与 android/app/build/（后者曾积到 2.2 GB）。
# Gradle 的缓存与构建中间产物同理，全部可由源码重建，不进版本库。
build/
.gradle/
android/.gradle/
android/app/release/
*.apk
*.aab

# RN native 的 CMake 中间产物。单独列出来是因为它**不叫 build**，
# 光靠上面的 build/ 规则拦不住 —— 实测这一项就占 423 MB。
.cxx/
# Kotlin 编译器的会话缓存，同样是隐藏目录，不会与任何常规文件重名
.kotlin/

# ---------- 日志 ----------
*.log
*.err
logcat.txt

# ---------- 签名密钥（绝对不能进版本库）----------
# ⚠️ release.keystore 是发布签名密钥：
#   - 泄漏 → 任何人都能签出与你同签名的包；
#   - 丢失 → 已上架的 App 再也无法更新。
# 它只该存在于本机和安全备份里，所以这里显式忽略。
# （debug.keystore 一并忽略：它是自动生成的，没有保留价值。）
*.jks
*.keystore

# ---------- macOS ----------
.DS_Store
